# Account creation date logging Discord bot

Very small, simple bot that listens for user join events and logs the account creation date if the gap is smaller than our threshold.

## Commands
```
~add_date
~add_dates_between
~remove_date  OR  ~delete_date
~remove_dates_between  OR  ~delete_dates_between
~dates
```
